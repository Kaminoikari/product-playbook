# Eval Debt Report — 2026-05-27

> Source: `evals/quick-results.json`  ·  Kind: `behavioral`

## Snapshot

- **Score:** 0/100  ·  Band: `at-risk`
- **Passed:** 34 / 64
- **Critical / Warning / Info failures:** 10 / 19 / 1
- **Genuine failing clusters:** 7  ·  ⚠️ Infra errors: 16

## ⚠️ Infrastructure errors (re-run before fixing)

16 failing expectation(s) across 3 eval(s) look like **test-harness** failures (claude subprocess timeout, judge couldn't parse output) — NOT real skill regressions. Re-run the eval before treating these as work items.

- **eval-jtbd-depth** — 7 expectation(s) affected
  - _signature:_ `response error: Command '['claude', '-p', "I'm building a B2B HR leave management system that primarily serves two user types: HR Specialist…`
- **eval-security-awareness** — 4 expectation(s) affected
  - _signature:_ `response error: Command '['claude', '-p', "I've completed the product planning (Full Mode) for a B2C social platform. The platform has user …`
- **eval-subagent-premortem** — 5 expectation(s) affected
  - _signature:_ `judge error: judge returned non-JSON: I need your approval to stage and commit these two files (`install.sh` and `package.json`). The change…`

## Where to invest first

**Top 3 to fix first** (highest severity-weight):

1. **eval-subagent-strategy-critic** (weight 35) → `agents/strategy-critic.md`
   Rumelt diagnosis identification; quote specific sentence; no-rewrite enforcement.
2. **eval-mode-selection** (weight 15) → `SKILL.md`
   Onboarding Step 1 (Confirm mode) — neutral menu, no single-mode push.
3. **eval-prfaq-output** (weight 15) → `references/04a-prfaq.md`
   PR-FAQ solution paragraph opens with user feel; ≥1 ❌ Hard Gate in self-check.

## All failing clusters (ranked by severity weight)

### eval-subagent-strategy-critic  (weight 35)

**Severity:** 🔴 2 critical · 🟡 1 warning

**Where to look:**
- `agents/strategy-critic.md` *(primary)*

**Likely root cause:** Rumelt diagnosis identification; quote specific sentence; no-rewrite enforcement.

**Failing expectations:**
- 🔴 The critique scores the diagnosis as missing or weak, explaining that the strategy names no central challenge (it states a goal and an aspiration instead)
  - _judge:_ Response never frames the critique around a missing 'diagnosis' or 'central challenge'; it critiques vagueness and trade-offs but does not say the strategy states a goal/aspiration instead of identifying a challenge.
- 🟡 Each critique points at a specific quoted sentence from the strategy and cites which principle is violated, rather than giving generic feedback
  - _judge:_ Each section quotes the strategy element but does not cite a named principle violated (e.g., 'strategy requires trade-offs' is implied, not cited as a principle); the expectation requires explicitly citing which principle is violated.
- 🔴 The response returns strengthening questions for the writer and does NOT rewrite the strategy on the user's behalf
  - _judge:_ The response offers to 'help rebuild this using a structured framework' but does not pose strengthening questions for the writer to answer themselves; the expectation requires questions, not a rewrite offer.

### eval-mode-selection  (weight 15)

**Severity:** 🔴 1 critical

**Where to look:**
- `SKILL.md` *(primary)*
- `references/rules-commands.md` (secondary)

**Likely root cause:** Onboarding Step 1 (Confirm mode) — neutral menu, no single-mode push.

**Failing expectations:**
- 🔴 Response presents modes as a clear selection menu (e.g., numbered options or table); does not jump straight into giving directional advice or starting analysis (no JTBD outline, no framework recommendations)
  - _judge:_ While a table is presented, the response gives directional advice by recommending Quick Mode ('I'd recommend Quick Mode') and explaining why it fits, rather than neutrally presenting options.

### eval-prfaq-output  (weight 15)

**Severity:** 🟡 3 warning

**Where to look:**
- `references/04a-prfaq.md` *(primary)*
- `references/rules-quality-review.md` (secondary)

**Likely root cause:** PR-FAQ solution paragraph opens with user feel; ≥1 ❌ Hard Gate in self-check.

**Failing expectations:**
- 🟡 The first sentence of the solution paragraph starts with user experience or a usage scenario, not with a system-feature verb (must not begin with 'PrepFlow lets you', 'The system will', 'Click to', or similar feature-first phrasing)
  - _judge:_ Solution paragraph opens with 'PrepFlow connects three things...', a system-feature verb describing what the product does, not a user experience or usage scenario.
- 🟡 External FAQ contains at least 1 pointed challenge comparing to existing tools (e.g., 'Why not just keep using Excel or spreadsheets?'), and the answer must explicitly acknowledge at least one specific advantage of the existing tool (e.g., 'Excel is flexible and kitchen staff are already familiar with it') — vague acknowledgments like 'if it works for you, keep using it' are insufficient
  - _judge:_ The External FAQ contains no question comparing PrepFlow to spreadsheets/Excel or other existing tools; the spreadsheet comparison appears only in the Internal FAQ (Q2), not in the External FAQ section.
- 🟡 Quality self-review checklist contains at least 1 explicit cross/fail marker (warning markers do not count), and that failed item includes a specific improvement direction
  - _judge:_ The self-review uses only ⚠️ warning markers for the three weaknesses; there are no explicit cross/fail markers (e.g., ❌), which the expectation requires (warning markers do not count).

### eval-quality-hardgate  (weight 15)

**Severity:** 🟡 3 warning

**Where to look:**
- `references/rules-quality-review.md` *(primary)*
- `references/02b-jtbd.md` (secondary)

**Likely root cause:** ≥1 ❌ in self-check with fix direction; Q5 emotional vocab not 'metacognitive'.

**Failing expectations:**
- 🟡 Quality self-review checklist contains at least 1 explicit cross/fail marker (warning markers do not count) — proving the self-review mechanism genuinely works and is not a rubber stamp
  - _judge:_ All items are marked ✅ or ⚠️; no explicit cross/fail (❌/FAIL/✗) marker appears anywhere in the checklist.
- 🟡 Failed items include a specific improvement explanation (describing how to strengthen it, not just marking it as failed without text)
  - _judge:_ No item carries an explicit fail marker, so no failed item exists to include an improvement explanation; the ⚠️ warning item is excluded per expectation 1's criteria.
- 🟡 The final question in the Five Whys (Q5) uses explicit emotional vocabulary (e.g., fear, anxiety, shame, worry, dread, self-doubt, sense of loss) rather than merely describing functional consequences
  - _judge:_ Layer 5 reads 'Most apps are either dumb logs…neither creates the low-friction, high-signal feedback loop' — purely functional; no fear, anxiety, shame, dread, or similar emotional vocabulary.

### eval-subagent-discovery  (weight 15)

**Severity:** 🔴 1 critical

**Where to look:**
- `agents/discovery-specialist.md` *(primary)*

**Likely root cause:** B2B branch — separate Buyer Persona vs User Persona.

**Failing expectations:**
- 🔴 For this B2B product, the buyer persona and the daily-user persona are treated as separate roles
  - _judge:_ Persona B conflates buyer and daily user as the same role; Open Question #2 raises the split as unresolved rather than treating them as distinct analyzed personas.

### eval-context-bootstrap  (weight 6)

**Severity:** 🟡 1 warning · 🔵 1 info

**Where to look:**
- `references/rules-context.md` *(primary)*
- `references/rules-context-template.md` (secondary)

**Likely root cause:** Reuse declared stack; mention .product-context.md persistence.

**Failing expectations:**
- 🟡 Confirms or references the tech stack already provided by the user (React + Node.js + MongoDB) without asking for re-description
  - _judge:_ Response makes no mention of React, Node.js, or MongoDB; the tech stack provided by the user is entirely absent from the response.
- 🔵 Mentions that product context will be saved for future use (or references .product-context.md)
  - _judge:_ Response never mentions saving product context, .product-context.md, or any future reuse of the provided information.

### eval-revision-mode  (weight 5)

**Severity:** 🟡 1 warning

**Where to look:**
- `references/rules-revision.md` *(primary)*

**Likely root cause:** Revision Mode post-S1 — concrete CTA menu, not open question.

**Failing expectations:**
- 🟡 After S1, provides the user with clear action options (numbered menu or specific CTAs), not an open-ended question (e.g., must not end with just 'Any thoughts?')
  - _judge:_ Response ends with four open-ended data collection questions, not a numbered action menu or CTAs offering choices like 'proceed to S2' or 'deep dive on retention'.

---

## Notes

- This report is **deterministic attribution**, not patch generation. Stage 2 of the self-improvement loop will propose actual diffs, still gated by human PR review.
- Infra errors (claude subprocess timeout, judge non-JSON) are split out so they don't pollute fix backlog — re-run the eval to confirm whether they were transient.
- If an eval lacks a `Where to look` pointer, it means `EVAL_ATTRIBUTION` in `scripts/eval-debt-report.py` needs an entry.
- Judge quotes show the first reason only; the full per-run reasoning lives in the source JSON.
